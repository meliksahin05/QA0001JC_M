package d8Ternary_StringManipulation;

public class Ternary01 {

    public static void main(String[] args) {

        //another conditional stament sytanx ternary


//        (condition)  ?  (this part only for true) : (this part is working only for false);

        // in ternary: we need to keep returning data in related data type then we can print......


           //Example create a ternary to print absolute value of given number

        int x = -9;

         int absValue = (x<0) ? (x * (-1)) : (x);

        System.out.println("absValue = " + absValue);


    }
}
